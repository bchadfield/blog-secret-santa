<% title "#{@pool.name} Elf Area" %>

<div class="container">
	<div class="row">
		<div class="col-sm-12">
			<div class="content center">
				<h1><%= yield :heading %></h1>
			</div>
			<ul class="users col-sm-4">
	      <% @pool.users.each do |user| %>
	      	<%= render "users/user", user: user, display_email: true %>
	      <% end %>
      </ul>
		</div>
	</div>
</div>